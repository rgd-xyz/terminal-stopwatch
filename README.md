# a big ol and simple stopwatch

installation

clone the repo:

```bash
git clone https://github.com/rgd-xyz/terminal-stopwatch.git
cd terminal-stopwatch

run the installation script:
chmod +x install.sh
./install.sh
